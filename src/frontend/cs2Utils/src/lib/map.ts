export interface MapInfo {
  name: string;
  id: string;
  asset_name: string;
}

export interface Team {
  name: string;
  id: string;
  asset_name: string;
}

export interface Stuff {
  name: string;
  id: string;
  asset_name: string;
}